import React from "react";

const ShowBills = (props) => {

    // all customer and add dependencies = like customers , products from store
    return (
        <div>

        </div>
    )
}

export default ShowBills